# Dev/test tools — NOT installed in the Docker image.
# Used by `make test-kg` (local venv) and `make compose-test-kg` (in-container).
-r requirements.txt
ruff>=0.4.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
