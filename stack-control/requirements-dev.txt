# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (C) 2026 Lumogis
# Dev/test tools — NOT installed in the stack-control Docker image.
# These packages are for local development and CI only.
#
# Install with (from repo root):
#   pip install -r stack-control/requirements-dev.txt
# Run tests with:
#   cd stack-control && python -m pytest test_main.py -q
#
-r requirements.txt
httpx>=0.27.0
pytest>=8.0.0
