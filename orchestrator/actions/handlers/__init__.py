# Concrete action handlers live here.
# Core ships no handlers — they are contributed by lumogis-app and plugins.
# Each handler file registers its ActionSpec(s) via actions.registry.register_action().
