# Full Docker/Compose runtime profile — requires requirements-core.txt beside this file.
# Hub PyInstaller bundled freeze installs requirements-core.txt only (no sentence-transformers).
-r requirements-core.txt
# Optional BGE cross-encoder reranker (RERANKER_BACKEND=bge in Compose).
sentence-transformers>=5.0.0
