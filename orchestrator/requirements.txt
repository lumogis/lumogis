# Runtime dependencies — installed in the Docker image.
# Dev/test tools live in requirements-dev.txt and are NOT installed in Docker.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
anthropic>=0.80.0
openai>=2.0.0
httpx>=0.27.0
pyyaml>=6.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
# packaging is used by services/capability_registry.py for min_core_version
# comparison via packaging.version.Version. Universally shipped via pip,
# declared explicitly so we don't rely on transitive installation.
packaging>=21.0
qdrant-client>=1.12.0
psycopg2-binary>=2.9.0
pdfminer.six>=20231228
tiktoken>=0.10.0
sentence-transformers>=5.0.0
pytesseract>=0.3.10
python-docx>=1.0.0
pdf2image>=1.17.0
watchdog>=5.0.0
psutil>=6.0.0
pyjwt>=2.8.0
feedparser>=6.0.0
trafilatura>=2.0.0
apscheduler>=3.10.0
caldav>=1.3.0
# mcp 1.10.0 was the first stable streamable-HTTP release; the orchestrator
# uses it via mcp_server.py to expose Lumogis as an MCP server at /mcp.
# Pulls pyjwt[crypto] (and therefore the `cryptography` wheel ~5MB).
mcp>=1.10.0
