services:
  orchestrator:
    build: ./orchestrator
    volumes:
      - ./orchestrator:/app
      - ${FILESYSTEM_ROOT:-/tmp/lumogis-data}:/data:ro
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
