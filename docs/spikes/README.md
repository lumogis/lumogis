# Browser-origin provider spikes (Chunk 0)

Non-product artefacts for **Lumogis Mobile Cloud Fallback + Sync** (`lumogis_mobile_cloud_fallback_sync`).  
**Executable harnesses:** `../../scripts/spikes/` — **never** imported by `clients/lumogis-web/src` production bundles.

| Doc | Purpose |
|-----|---------|
| `anthropic_browser_origin_proof.md` | Anthropic **`spike_status`** evidence |
| `openai_browser_origin_proof.md` | OpenAI **`blocked_by_default`** rationale (Chunk 0) |
